#include <bits/stdc++.h>
using namespace std;

#ifdef LOCAL
#define DEBUG
#include "debug/debug_2.h"
#else
#define dbg(x...)
#endif

#define int long long

void solve (int tid) {
   
}
 
int32_t main() {
  ios::sync_with_stdio(false);
  cin.tie(0);
  int tt = 1;
  cin >> tt;
  for (int i = 1; i <= tt; i++) {
      solve(i);
  }
  return 0;
}